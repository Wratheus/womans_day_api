services:
  api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - db-data:/app/data
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/data/womansday.db
    restart: unless-stopped

volumes:
  db-data:
